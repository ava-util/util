{
  "name": "@kaseano/util",
  "version": "1.0.2",
  "description": "Used to get config from json file",
  "scripts": {
    "prepack": "yarn test",
    "prepare": "yarn tsc",

    "build-dev": "webpack",
    "build": "cross-env NODE_ENV=production webpack",
    "watch": "tsc -w",
    "tsc": "rimraf dist/* && tsc",
    "test": "mocha",
    "test-tsconfig": "SET TS_NODE_PROJECT=./tsconfig.testing.json&& mocha",
    "test-babel": "mocha --require @babel/register",
    "test-brk": "mocha --inspect-brk"
  },
  "author": "Sean Stockson",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kaseano/util.git"
  },
  "exports_bak": "./dist/index.js",
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.11.6",
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
